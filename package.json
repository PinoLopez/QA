{
  "name": "qa-project",
  "version": "1.0.0",
  "description": "Automatización de pruebas con Playwright y Artillery",
  "scripts": {
    "test": "npx playwright test",
    "install-playwright": "npx playwright install --with-deps",
    "artillery": "artillery run artillery.yml -o artillery-report.json",
    "artillery-report": "artillery report artillery-report.json --output artillery-report.html"
  },
  "devDependencies": {
    "@playwright/test": "^1.37.1",
    "@types/node": "^18.0.0",
    "artillery": "^2.0.0-23",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "@faker-js/faker": "^8.0.2",
    "dotenv": "^16.3.1"
  }
}
